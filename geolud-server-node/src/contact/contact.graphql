type Mutation {
    submitContactRequest(contactRequest: ContactRequest): String
}

input ContactRequest {
    name: String
    email: String!
    message: String!
}

type Subscription {
    contactRequestSubmitted: string
}
