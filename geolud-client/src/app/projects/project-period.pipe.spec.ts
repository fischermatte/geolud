import { ProjectPeriodPipe } from './project-period.pipe';

describe('ProjectPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
