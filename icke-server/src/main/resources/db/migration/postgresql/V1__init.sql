CREATE TABLE LINK (URL VARCHAR(1024) NOT NULL, TITLE VARCHAR(1024) NOT NULL, PROJECT_ID VARCHAR(40) NOT NULL, PRIMARY KEY (PROJECT_ID, URL, TITLE));
CREATE TABLE PROJECT (ID VARCHAR(40) NOT NULL, CUSTOMER_NAME VARCHAR(255), CUSTOMER_URL VARCHAR(255), DESCRIPTION TEXT, INTERVAL_FROM DATE, INTERVAL_TO DATE, TITLE VARCHAR(255), PRIMARY KEY (ID));
ALTER TABLE LINK ADD CONSTRAINT FK_LINK_PROJECT FOREIGN KEY (PROJECT_ID) REFERENCES PROJECT;